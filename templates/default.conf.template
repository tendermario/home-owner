server {
    listen       80;
    server_name  localhost;

    location /api/ {
        rewrite ^/api/(.*) /$1 break;
        proxy_pass  http://api:8000;
    }

    location / {
        proxy_pass  http://frontend:3000;
    }
}
